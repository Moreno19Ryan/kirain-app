import 'package:flutter/material.dart';

class CatatScreen extends StatelessWidget {
  const CatatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Catat')));
  }
}
