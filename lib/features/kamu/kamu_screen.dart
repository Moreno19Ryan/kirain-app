import 'package:flutter/material.dart';

class KamuScreen extends StatelessWidget {
  const KamuScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Kamu')));
  }
}
